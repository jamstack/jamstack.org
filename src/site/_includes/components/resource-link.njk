    <li class="mt-6">
      <h4 class="text-lg font-bold"><a href="{{ item.url }}">{{ item.title }}</a></h4>
      <time value="{{ item.date }}" class="text-gray-600">{{ item.date }}</time>
      <p>{{ item.synopsis }}</p>
    </li>
