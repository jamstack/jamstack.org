<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Jamstack : {{ title }}</title>
  </head>
  <body class="bg-blue-900 text-blue-100 ">
    {% include "banner.njk" %}
    <div class="container max-w-4xl lg:max-w-5xl mx-auto py-8 md:px-16 leading-relaxed antialiased">
      {% include "header.njk" %}
      {{ content | safe }}
    </div>
    <div class="mt-32 bg-white pt-12 pb-12 mt">
      <div class="container max-w-4xl lg:max-w-5xl mx-auto py-8 md:px-16 leading-relaxed antialiased">
      {% include "footer.njk" %}
      </div>
    </div>
  </body>
</html>
